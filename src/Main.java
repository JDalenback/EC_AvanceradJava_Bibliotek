import models.Librarian;
import models.Library;
import models.User;

public class Main {
    public static void main(String[] args) {
        Librarian.addBook();
        Library.readBookList();
    }
}
